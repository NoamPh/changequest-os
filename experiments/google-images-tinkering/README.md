# Google Images Tinkering

A sandbox for experimenting with Google Images — image search, fetching,
and playing with results. Kept separate from the main ChangeQuest OS app so
experiments stay isolated and disposable.

## Status

Scaffold only. Nothing here yet beyond this workspace.

## Ideas to try

- Query the image search and inspect the response shape
- Download and cache result thumbnails
- Build a tiny React view to browse results

## Notes

Add API keys / secrets via environment variables — never commit them.
